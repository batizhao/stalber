# 代码生成
gen: 
  # 作者
  author: batizhao
  # 默认项目模板 pecado
  projectKey: pecado
  # 默认生成包路径 me.batizhao
  packageName: me.batizhao
  # 默认生成模块名 system
  moduleName: system
  # 默认pojo包名 domain
  pojoPackageName: domain
  # 默认mapper包名 mapper
  mapperPackageName: mapper
